# dependencies
node_modules/
.pnp
.pnp.js

# build outputs
dist/
dist-electron/
release/
*.app
*.exe
*.dmg

# data
*.db
*.db-journal
*.db-wal
*.db-shm

# env
.env
.env.local
.env.*.local

# logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*

# editor
.vscode/*
!.vscode/extensions.json
.idea/
*.swp
*.swo

# os
.DS_Store
Thumbs.db

# cache
.vite/
.turbo/
.eslintcache

# test/verification artifacts (regenerated on every run)
.self-test-result.json
.ui-test-result.json
ui-screenshot.png

# local-only: research notes referencing external projects (kept out of public repo)
.zcode-reference/

# local agent working files (plans, session state) — per-developer, not project content
.zcode/

# development-process docs (architecture, build notes, roadmap, historical design plans)
# NOT user-facing. User-facing docs live in docs/. See AGENTS.md "Project rules".
dev-docs/

# local memory (per-developer agent memory)
memory/
